pipeline{
    agent any
    stages{
       stage('Git checkout') {
            steps {
                git branch: 'project-1', url: 'https://github.com/Pushkar8860/Spring-boot-deployment.git'
            }
        }
    }
}